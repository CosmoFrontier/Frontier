{
  "name": "web",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "rimraf dist && webpack --watch --devtool=inline-source-map --mode=development",
    "build": "rimraf dist && webpack --mode=production"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^10.2.0",
    "webpack": "^5.65.0",
    "webpack-cli": "^4.9.1"
  },
  "dependencies": {
    "postprocessing": "^6.23.3",
    "three": "^0.136.0"
  }
}